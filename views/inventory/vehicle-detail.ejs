<li class="bg-white rounded shadow p-4">
  <img src="<%= vehicle.inv_thumbnail %>" alt="<%= vehicle.inv_make %> <%= vehicle.inv_model %>" class="mb-2">
  <h2 class="font-bold"><%= vehicle.inv_make %> <%= vehicle.inv_model %></h2>
  <p>Preço: $<%= vehicle.inv_price %></p>
  <a href="/inventory/detail/<%= vehicle.inv_id %>" class="text-blue-600 underline">Ver detalhes</a>
</li>
